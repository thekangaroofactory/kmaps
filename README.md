# kmaps

Map Related Functions. \
The package wraps a set of functions related to Leaflet use cases.

## Installation

Install the kmaps package from GitHub as follows:

``` r
pak::pkg_install("thekangaroofactory/kmaps")
```
